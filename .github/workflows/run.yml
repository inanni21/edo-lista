name: Compilar e Executar EDO

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Compilar
        run: gcc -o edo edo_lista.c -lm
      - name: Executar
        run: ./edo
